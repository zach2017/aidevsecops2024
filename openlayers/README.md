# OpenLayers Demos

Experiments with the OpenLayers mapping library. The `demo` folder provides a Vite + React example, while other files show standalone JavaScript and TypeScript approaches.

## Running the React demo
```bash
cd demo
npm install
npm run dev
```
