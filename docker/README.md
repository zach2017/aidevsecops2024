# Docker Examples

This directory hosts container-related demos.
The `simpledemo` subfolder contains a full stack example with a Next.js frontend and a Spring Boot backend. The services can be run together with Docker Compose.

## Running the demo
```bash
docker-compose -f simpledemo/docker-compose up --build
```

See the individual subfolders for additional Dockerfiles and instructions.
